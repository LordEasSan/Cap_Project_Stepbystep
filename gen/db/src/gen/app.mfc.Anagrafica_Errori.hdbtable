-- generated by cds-compiler version 3.5.4
COLUMN TABLE app_mfc_Anagrafica_Errori (
  ID_ERRORE INTEGER NOT NULL,
  MESSAGGIO_ERRORE NVARCHAR(1000) NOT NULL,
  DATA_ERRORE SECONDDATE NOT NULL,
  STATO_BOTTONE BOOLEAN NOT NULL
) WITH ASSOCIATIONS (
  MANY TO ONE JOIN app_mfc_Anagrafica_Flussi AS ID_FLUSSO ON (ID_FLUSSO),
  MANY TO ONE JOIN app_mfc_Anagrafica_Stato_Errori AS ID_STATO_ERRORE ON (ID_STATO_ERRORE),
  MANY TO ONE JOIN app_mfc_Anagrafica_Utenti AS ID_UTENTE ON (ID_UTENTE)
)